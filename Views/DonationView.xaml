<UserControl x:Class="BloodBankPro.Views.DonationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Loaded="OnLoaded" Background="#F9F9F9">
    <Grid>
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
        <Border Grid.Row="0" Background="#FFFFFF" Padding="24,16" BorderBrush="#E0E4E8" BorderThickness="0,0,0,1">
            <Border.Effect><DropShadowEffect ShadowDepth="1" BlurRadius="6" Opacity="0.04" Color="Black"/></Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="{DynamicResource Page_Donations}" Style="{StaticResource PageHeader}"/>
                    <TextBlock Text="Record donations — auto-fills blood type, checks 56-day eligibility" Style="{StaticResource PageSub}"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="📤 Export" Style="{StaticResource IconBtn}" Margin="0,0,8,0" Click="BtnExport_Click"/>
                    <Button Content="+ Record Donation" Style="{StaticResource PrimaryBtn}" Click="BtnAdd_Click"/>
                </StackPanel>
            </Grid>
        </Border>
        <Grid Grid.Row="1" Margin="20">
            <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
            <Border Grid.Row="0" Style="{StaticResource Card}" Padding="14,10" Margin="0,0,0,12">
                <Border.Effect><DropShadowEffect ShadowDepth="1" BlurRadius="6" Opacity="0.04" Color="Black"/></Border.Effect>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="🔍" FontSize="14" Foreground="#5A6A7A" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBox x:Name="TxtSearch" Height="34" Width="220" Padding="8,6" FontSize="13"
                             Background="#FFFFFF" Foreground="#1A1A2E" BorderBrush="#D5D8DC" BorderThickness="1"
                             CaretBrush="#8B0000" Margin="0,0,10,0" TextChanged="OnFilter"/>
                    <ComboBox x:Name="CmbBlood" Style="{StaticResource FCB}" Height="34" Width="115" SelectionChanged="OnFilter">
                        <ComboBoxItem Content="All Types" IsSelected="True"/>
                        <ComboBoxItem Content="A+"/><ComboBoxItem Content="A-"/><ComboBoxItem Content="B+"/><ComboBoxItem Content="B-"/>
                        <ComboBoxItem Content="AB+"/><ComboBoxItem Content="AB-"/><ComboBoxItem Content="O+"/><ComboBoxItem Content="O-"/>
                    </ComboBox>
                    <TextBlock x:Name="TxtCount" Text="" FontSize="12" Foreground="#8795A5" VerticalAlignment="Center" Margin="16,0,0,0"/>
                </StackPanel>
            </Border>
            <Border Grid.Row="1" Style="{StaticResource Card}">
                <Border.Effect><DropShadowEffect ShadowDepth="2" BlurRadius="10" Opacity="0.06" Color="Black"/></Border.Effect>
                <DataGrid x:Name="DgDonations" Style="{StaticResource DarkGrid}"
                          RowStyle="{StaticResource DarkRow}" CellStyle="{StaticResource DarkCell}">
                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#F6F8FA"/>
                            <Setter Property="Foreground" Value="#6A1B9A"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="FontFamily" Value="Segoe UI"/>
                            <Setter Property="Padding" Value="10,0"/>
                            <Setter Property="BorderBrush" Value="#E0E4E8"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID"            Binding="{Binding Id}"                               Width="45"/>
                        <DataGridTextColumn Header="Donor Name"    Binding="{Binding DonorName}"                        Width="180"/>
                        <DataGridTextColumn Header="Blood Type"    Binding="{Binding BloodType}"                        Width="100"/>
                        <DataGridTextColumn Header="Units"         Binding="{Binding UnitsDonated}"                     Width="70"/>
                        <DataGridTextColumn Header="Date"          Binding="{Binding DonationDate, StringFormat='yyyy-MM-dd'}" Width="120"/>
                        <DataGridTextColumn Header="Recorded By"   Binding="{Binding RecordedBy}"                       Width="120"/>
                        <DataGridTextColumn Header="Notes"         Binding="{Binding Notes}"                            Width="*"/>
                        <DataGridTemplateColumn Header="Actions" Width="90">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Delete" Style="{StaticResource DelBtn}" Click="BtnDelete_Click" Tag="{Binding}" Margin="4,2"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>
        <Grid x:Name="Overlay" Grid.RowSpan="2" Visibility="Collapsed" Background="#88000000">
            <Border Background="#FFFFFF" Width="470" CornerRadius="14" VerticalAlignment="Center" BorderBrush="#6A1B9A30" BorderThickness="1">
                <Border.Effect><DropShadowEffect ShadowDepth="4" BlurRadius="20" Opacity="0.15" Color="Black"/></Border.Effect>
                <StackPanel Margin="32,26">
                    <TextBlock Text="Record New Donation" FontSize="18" FontWeight="Bold" FontFamily="Segoe UI Semibold" Foreground="#6A1B9A" Margin="0,0,0,18"/>
                    <TextBlock Text="DONOR *" Style="{StaticResource FL}"/>
                    <ComboBox x:Name="FDonor" Style="{StaticResource FCB}" DisplayMemberPath="FullName" SelectedValuePath="Id" SelectionChanged="FDonor_Changed"/>
                    <TextBlock Text="BLOOD TYPE" Style="{StaticResource FL}"/>
                    <ComboBox x:Name="FBlood" Style="{StaticResource FCB}">
                        <ComboBoxItem Content="A+"/><ComboBoxItem Content="A-"/><ComboBoxItem Content="B+"/><ComboBoxItem Content="B-"/>
                        <ComboBoxItem Content="AB+"/><ComboBoxItem Content="AB-"/><ComboBoxItem Content="O+"/><ComboBoxItem Content="O-"/>
                    </ComboBox>
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="12"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="UNITS DONATED *" Style="{StaticResource FL}"/>
                            <TextBox x:Name="FUnits" Style="{StaticResource FTB}" Text="1"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="DONATION DATE *" Style="{StaticResource FL}"/>
                            <DatePicker x:Name="FDate" Height="40" Margin="0,0,0,12" FontSize="13" Background="#FFFFFF" Foreground="#1A1A2E"/>
                        </StackPanel>
                    </Grid>
                    <TextBlock Text="NOTES" Style="{StaticResource FL}"/>
                    <TextBox x:Name="FNotes" Style="{StaticResource FTB}" Height="60" TextWrapping="Wrap" AcceptsReturn="True"/>
                    <Border x:Name="EligBorder" Background="#FFF8E1" CornerRadius="8" Padding="12,10"
                            Margin="0,0,0,10" Visibility="Collapsed" BorderBrush="#E6510030" BorderThickness="1">
                        <TextBlock x:Name="EligText" FontSize="12" Foreground="#E65100" TextWrapping="Wrap"/>
                    </Border>
                    <TextBlock x:Name="FError" Text="" Foreground="#8B0000" FontSize="12" Margin="0,0,0,8" Visibility="Collapsed" TextWrapping="Wrap"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                        <Button Content="Cancel" Style="{StaticResource CancelBtn}" Margin="0,0,10,0" Click="BtnCancel_Click"/>
                        <Button Content="💾  Save Donation" Style="{StaticResource PrimaryBtn}" Background="#6A1B9A" Click="BtnSave_Click"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>

